name: Chezmoi CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:


# Explicit rather than inherited: the repo default is read-only today, but a
# settings change (or a fork with a permissive default) should not silently
# hand these jobs a write token. Nothing here needs more than a checkout.
permissions:
  contents: read

# One run per ref (#132): a force-push or a quick PR update cancels the stale
# run instead of racing it to completion (two runs 63s apart both ran to
# completion on 2026-09-24, burning two full matrix passes).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Path gate for the heavy jobs (#132). GitHub's own paths/paths-ignore
  # filters are workflow-level only - a workflow-level ignore for **.md
  # would also skip the lint job, and the docs-consistency tests living
  # there are exactly the tests that must gate a docs change. So the gate
  # lives in this job instead: heavy jobs (Windows runners, Docker, the
  # apply matrix) are skipped for docs-only changes, lint always runs.
  changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      heavy: ${{ steps.diff.outputs.heavy }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0 # three-dot diff needs the merge base

      - id: diff
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          if [ "${EVENT_NAME}" != "pull_request" ]; then
              echo "heavy=true" >> "$GITHUB_OUTPUT"
              exit 0
          fi
          if git diff --name-only "origin/${BASE_REF}...HEAD" | grep -qvE '^(docs/|[^/]+\.(md|markdown)$)'; then
              echo "heavy=true" >> "$GITHUB_OUTPUT"
          else
              echo "Docs-only change - heavy jobs skipped; lint still gates it."
          fi

  lint:
    name: Lint & Validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # zsh: without it, the zshrc contract's `zsh -n` syntax check silently
      # took its SKIP branch here (this runner image has no zsh - the other two
      # jobs install it explicitly), leaving only greps and shellcheck.
      - name: Install shellcheck and zsh
        run: sudo apt-get update && sudo apt-get install -y shellcheck zsh

      # Without chezmoi on PATH, every contract test that renders a template
      # early-returns ("PASS/SKIP: ... requires chezmoi") - this job was green
      # while skipping the rendering assertions entirely (confirmed live: the
      # skill-wiring contract skipped 3 blocks and validated only greps).
      # Pinned to .chezmoi-version, like the other jobs.
      - uses: ./.github/actions/install-chezmoi

      - name: Lint shell scripts
        run: |
          # Extract and lint bash scripts from templates.
          # perl -0777 (slurp whole file) + the 's' modifier (dot matches newline) is
          # required, not just a style choice: a plain per-line `sed 's/{{[^}]*}}//g'`
          # cannot span a multi-line {{/* ... */}} comment, silently leaking its prose
          # text through as "bash code" for shellcheck to choke on - confirmed live,
          # this exact thing happened with a real multi-line comment in this repo.
          # Widened in #131 from `run_onchange_*.sh.tmpl install.sh
          # devcontainer/install.sh` to every shell file git tracks: the same
          # audit found 6 of 53 shell files linted and real findings in the
          # rest (all fixed on the commit that introduced this loop).
          # Per-class exclusions, kept explicit on purpose:
          #   SC1091  "not following" every dynamic `. lib.sh` / lib/*.sh source -
          #           the sourced files are in-repo; following them needs -x and
          #           buys nothing for contract greps.
          #   SC1128  every OS-gated *.sh.tmpl starts with a `{{ if }}` wrapper
          #           line, so the stripped text has no shebang on line 1 - a
          #           structural property of templating, not a defect.
          #   SC2016  tests/*.sh quote literal-$ snippets OF THE CODE UNDER
          #           TEST on purpose (that is what they assert about).
          #   SC2148  dot_aliases.zsh is a sourced file with no shebang by
          #           design; shellcheck has no zsh dialect, so it is parsed
          #           as bash (-s bash) with the same alias grammar.
          FAILED=0
          for f in $(git ls-files '*.sh' '*.sh.tmpl' 'dot_local/bin/executable_*' '*.zsh'); do
            [ -f "$f" ] || continue
            echo "Checking $f..."
            case "$f" in
              *.tmpl)
                perl -0777 -pe 's/\{\{.*?\}\}//gs' "$f" | shellcheck -s bash -e SC1091 -e SC1128 - || FAILED=1
                ;;
              tests/*.sh)
                shellcheck -s bash -e SC1091 -e SC2016 "$f" || FAILED=1
                ;;
              *.zsh)
                shellcheck -s bash -e SC1091 -e SC2148 "$f" || FAILED=1
                ;;
              *)
                shellcheck -s bash -e SC1091 "$f" || FAILED=1
                ;;
            esac
          done
          exit $FAILED

      - name: PSScriptAnalyzer (all .ps1 + stripped .ps1.tmpl)
        shell: pwsh
        run: |
          # The bash-side lint's PowerShell counterpart (#131: it did not run
          # anywhere). Templates are analyzed with the same strip idiom the
          # shellcheck loop uses. Rule exclusions, each deliberate:
          #   PSAvoidUsingWriteHost          profiles/installers print to the
          #                                  host on purpose (color, 5.1 quirks).
          #   PSUseBOMForUnicodeEncodedFile  the repo charset standard is
          #                                  UTF-8 *without* BOM (.editorconfig
          #                                  `charset = utf-8`, enforced by the
          #                                  init-line-endings tooling). Non-ASCII
          #                                  glyphs are cosmetic output, never
          #                                  parsed input.
          Install-Module PSScriptAnalyzer -RequiredVersion 1.25.0 -Force -Scope CurrentUser
          Import-Module PSScriptAnalyzer
          $failed = 0
          foreach ($f in (git ls-files '*.ps1' '*.ps1.tmpl')) {
            $target = $f
            if ($f -like '*.tmpl') {
              $stripped = (Get-Content $f -Raw) -replace '(?s)\{\{.*?\}\}', ''
              $target = Join-Path ([IO.Path]::GetTempPath()) (($f -replace '[\\/]', '__') + '.ps1')
              [IO.File]::WriteAllText($target, $stripped, [Text.UTF8Encoding]::new($false))
            }
            $findings = Invoke-ScriptAnalyzer -Path $target -Severity Error,Warning `
              -ExcludeRule PSAvoidUsingWriteHost, PSUseBOMForUnicodeEncodedFile
            if ($findings) {
              $findings | ForEach-Object {
                Write-Host "##[error]${f}:$($_.Extent.StartLineNumber) $($_.RuleName): $($_.Message)"
              }
              $failed = 1
            } else {
              Write-Host "ok: $f"
            }
          }
          exit $failed

      - name: actionlint (workflow lint)
        run: |
          # The three workflow files were validated only by execution before
          # (#132): a typo surfaced as a red run hours later, or worse as a
          # silently-skipped step. Version + checksum mirror the gitleaks
          # pattern below (#133: pinned download, verified before use).
          ACTIONLINT_VERSION=1.7.12
          curl -fsSL -o "/tmp/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz" "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz"
          curl -fsSL -o /tmp/actionlint_checksums.txt "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_checksums.txt"
          grep "actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz" /tmp/actionlint_checksums.txt |
            (cd /tmp && sha256sum -c -)
          tar -xzf "/tmp/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz" -C /tmp actionlint
          /tmp/actionlint -color

      - name: Secret/PII scan (gitleaks)
        # The tree-result.log class: debug dumps, machine paths, anything
        # credential-shaped - caught at PR time instead of after a manual
        # audit. The pinned binary runs directly (the gitleaks-action
        # wrapper's SARIF upload exits 1 on clean scans; the binary is
        # deterministic). Version comes from ONE place (.gitleaks-version,
        # shared with the pre-commit hook rev) and the tarball is verified
        # against the release's published checksums before it runs (#133;
        # the three versions here used to drift: v8.18.4 / 8.24.3 / latest).
        # --no-git scans the working tree; history scanning is a deliberate
        # follow-up (see .gitleaks.toml).
        run: |
          GITLEAKS_VERSION="$(cat .gitleaks-version)"
          curl -fsSL -o "/tmp/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl -fsSL -o "/tmp/gitleaks_${GITLEAKS_VERSION}_checksums.txt" \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_checksums.txt"
          grep "gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" "/tmp/gitleaks_${GITLEAKS_VERSION}_checksums.txt" |
            (cd /tmp && sha256sum -c -)
          tar -xzf "/tmp/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" -C /tmp gitleaks
          /tmp/gitleaks detect --source . --redact -v --no-git

      - name: Validate all TOML and YAML files
        # One loop over everything git tracks (#132): this used to validate
        # exactly two files (.chezmoiexternal.toml, .pre-commit-config.yaml)
        # while 5 fixture TOMLs, guardrail.toml, starship.toml, the data yaml
        # and the workflows went unchecked, via an unpinned `pip install toml`
        # (abandoned upstream, TOML 0.5). tomllib is stdlib on the runner's
        # Python 3.12; PyYAML ships on the runner image (no pip at all).
        run: |
          FAILED=0
          while IFS= read -r f; do
            # chezmoi modify-templates (e.g. dot_codex/modify_config.toml)
            # are templates first and TOML only after chezmoi merges them at
            # apply time - the raw source is not valid TOML by design.
            if grep -q 'chezmoi:modify-template' "$f"; then
              echo "Skipping $f (chezmoi modify-template)"
              continue
            fi
            echo "Validating $f..."
            case "$f" in
              *.toml) python3 -c "import sys, tomllib; tomllib.load(open(sys.argv[1], 'rb'))" "$f" || FAILED=1 ;;
              *)      python3 -c "import sys, yaml; yaml.safe_load(open(sys.argv[1]))" "$f" || FAILED=1 ;;
            esac
          done < <(git ls-files '*.toml' '*.yml' '*.yaml')
          exit $FAILED

      # One step runs every tests/*.sh (the runner enumerates the directory, so
      # a test cannot go unwired). --strict fails on any skip: this job installs
      # chezmoi, zsh and shellcheck above, so a skip here means a guard is too
      # broad or a dependency is missing. Run locally with TESTS_STRICT=0.
      - name: Test shell suite (all tests/*.sh, strict)
        run: bash tests/run.sh --strict

      - name: Test dotfiles doctor (pwsh twin)
        run: pwsh -NoProfile -File tests/dotfiles_doctor.ps1

      # select_packages.ps1 was once never wired and broken: the fake gum stub
      # inherited the file's CRLF, so its shebang carried a trailing CR and
      # Linux could not exec it. The bash runner does not enumerate .ps1 twins.
      - name: Test select-packages.ps1 (pwsh twin)
        run: pwsh -NoProfile -File tests/select_packages.ps1

      # The scripts under test run under powershell.exe (5.1, the documented
      # host) inside the test - see tests/dotbackup_restore.ps1.
      - name: Test dotbackup/restore round-trip (pwsh twin)
        run: pwsh -NoProfile -File tests/dotbackup_restore.ps1

      # Behavioral twin of tests/devprofile_contract.sh (Get-Accounts parsing,
      # Test-Identity dirs mapping) - needs pwsh 7, which the linux runner has.
      - name: Test devprofile identity (pwsh twin)
        run: pwsh -NoProfile -File tests/devprofile_identity.ps1

  test-unix:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: changes
    if: needs.changes.outputs.heavy == 'true'
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: ./.github/actions/install-chezmoi

      - name: Install zsh (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y zsh

      # Toolchain parity with the lint job (#132): once the suite itself is
      # macOS-portable (tests use GNU-only idioms like `stat -c '%a'` that
      # hard-fail on BSD stat rather than skip), these two installs plus
      # chezmoi above are everything `bash tests/run.sh --strict` needs here.
      - name: Install shellcheck (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install shellcheck

      - name: Verify chezmoi version
        run: ~/.local/bin/chezmoi --version

      - name: Test remote access package ownership
        if: matrix.os == 'ubuntu-latest'
        run: bash tests/remote_access_package_ownership.sh

      - name: Create test config from fixtures (single account, packages off)
        run: bash tests/fixtures/chezmoi/compose.sh single off --out ~/.config/chezmoi/chezmoi.toml

      - name: Test chezmoi apply (dry-run)
        run: ~/.local/bin/chezmoi apply --dry-run --verbose --source="${GITHUB_WORKSPACE}"

  test-windows:
    name: Test (Windows)
    runs-on: windows-latest
    needs: changes
    if: needs.changes.outputs.heavy == 'true'
    # 20 not 15 (#132): this job seeds configs and renders templates through
    # real installs; the one observed hang class (config prompt, tests/
    # check_workflow_config_keys.sh:8) needs a ceiling that a docs-lint-scale
    # job does not.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: ./.github/actions/install-chezmoi

      - name: Verify chezmoi
        run: |
          & "$env:USERPROFILE\.local\bin\chezmoi.exe" --version

      - name: Create test config from fixtures (single account, packages off)
        # bash on purpose: one composer for every runner. chezmoi.exe reads the
        # config under USERPROFILE, so the path is built from that rather than
        # from bash's own HOME, which a runner may point elsewhere.
        shell: bash
        run: bash tests/fixtures/chezmoi/compose.sh single off --out "$(cygpath -u "$USERPROFILE")/.config/chezmoi/chezmoi.toml"

      # #132: production runs these scripts under powershell.exe (5.1 - see
      # .chezmoi.toml.tmpl [interpreters.ps1]), yet the twins only ever ran
      # under pwsh on Ubuntu. Each twin declares #Requires -Version 5.1 and
      # was verified under a real 5.1 host before this step landed;
      # select_packages.ps1 self-skips without a console stdin on Windows
      # (its guard says so) and devprofile_identity.ps1 requires pwsh 7 -
      # both stay wired on the Linux lint job.
      - name: Test dotbackup/restore round-trip (5.1)
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File tests\dotbackup_restore.ps1

      - name: Test dotfiles doctor (5.1)
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File tests\dotfiles_doctor.ps1

      - name: Test pwsh profiles contract (5.1)
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File tests\pwsh_profiles.ps1

      - name: Verify PowerShell script syntax (every .ps1, every rendered .ps1.tmpl)
        shell: pwsh
        run: |
          # Parse what actually RUNS, not a text-stripped approximation.
          # This used to delete {{...}} with a negated character class and parse
          # the remains. That holds only while templates stay simple: once one
          # emits syntax AROUND its actions (a type-dispatching {{ if }} inside a
          # hashtable value), stripping leaves `'' = $"@()` - invalid PowerShell,
          # and a parse error that says nothing about the real script.
          # chezmoi renders the true file, so the check tests the artifact.
          # Widened in #131 from the single install_packages template to every
          # PowerShell file git tracks - the profiles (where the $args binding
          # bug lived) and all scripts/*.ps1 were never parsed before.
          $chezmoi = "$env:USERPROFILE\.local\bin\chezmoi.exe"
          $failed = 0
          foreach ($f in (git ls-files '*.ps1' '*.ps1.tmpl')) {
            $target = Join-Path $env:GITHUB_WORKSPACE $f
            if ($f -like '*.tmpl') {
              $rendered = & $chezmoi execute-template `
                --source="$env:GITHUB_WORKSPACE" `
                --file $target 2>&1
              if ($LASTEXITCODE -ne 0) {
                Write-Host "##[error]chezmoi could not render $f"
                Write-Host ($rendered | Out-String)
                $failed = 1
                continue
              }
              $target = "$env:TEMP\" + ($f -replace '[\\/]', '__') + '.rendered.ps1'
              $rendered | Out-File -FilePath $target -Encoding utf8
            }
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $target,
              [ref]$null,
              [ref]$errors
            ) | Out-Null

            if ($errors.Count -gt 0) {
              $errors | ForEach-Object { Write-Host "##[error]${f}:$($_.Extent.StartLineNumber): $($_.Message)" }
              $failed = 1
            } else {
              Write-Host "ok: $f"
            }
          }
          if ($failed -ne 0) { exit 1 }
          Write-Host "PowerShell syntax check passed on every .ps1 and every RENDERED template (0 errors)"

      - name: Test chezmoi apply (dry-run)
        run: |
          & "$env:USERPROFILE\.local\bin\chezmoi.exe" apply --dry-run --verbose --source="$env:GITHUB_WORKSPACE"

  devcontainer:
    name: Test Devcontainer
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.heavy == 'true'
    timeout-minutes: 15
    env:
      DEVCONTAINER: "true"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Syntax check devcontainer/install.sh
        run: |
          bash -n devcontainer/install.sh
          echo "Devcontainer script syntax OK"

      - uses: ./.github/actions/install-chezmoi

      - name: Run chezmoi apply (deliberately no pre-seeded account or packages)
        run: |
          # Every other job in this file pre-seeds ~/.config/chezmoi/chezmoi.toml
          # with an account and package toggles already set, which bypasses
          # .chezmoi.toml.tmpl's "no accounts key" -> devcontainer-defaults fallback
          # and its non-interactive package-toggle defaults entirely. This is the
          # one job that lets DEVCONTAINER=true drive real template behavior, so it
          # can actually verify isDevcontainer detection works end-to-end instead of
          # just parsing the wrapper script's syntax.
          #
          # `chezmoi apply` alone does NOT evaluate .chezmoi.toml.tmpl - that only
          # happens via `chezmoi init` (confirmed live: plain `apply` here failed
          # with "map has no entry for key accounts", because with no chezmoi.toml
          # on disk at all, `.` is just an empty map - every other job in this file
          # sidesteps this by pre-seeding a *static* chezmoi.toml directly, never
          # exercising .chezmoi.toml.tmpl's own logic either). Need `init --apply`.
          ~/.local/bin/chezmoi init --apply --source="${GITHUB_WORKSPACE}"

      - name: Verify devcontainer detection worked end-to-end
        run: |
          echo "=== Devcontainer placeholder account ==="
          grep -q 'name = "Devcontainer User"' ~/.gitconfig && echo "✓ Devcontainer User account created" || exit 1
          grep -q 'email = "devcontainer@local"' ~/.gitconfig && echo "✓ devcontainer@local email set" || exit 1

          echo "=== Package categories correctly defaulted non-interactive ==="
          grep -q 'core = false' ~/.config/chezmoi/chezmoi.toml && echo "✓ core defaulted false" || exit 1
          grep -q 'dev_desktop = false' ~/.config/chezmoi/chezmoi.toml && echo "✓ dev_desktop defaulted false" || exit 1

  #=============================================================================
  # TIER 1: Docker Smoke Test - Real chezmoi init/apply in fresh environment
  #=============================================================================
  smoke-test:
    name: Smoke Test (Docker)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.heavy == 'true'
    timeout-minutes: 15
    container:
      image: ubuntu:22.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y curl git zsh

      - uses: ./.github/actions/install-chezmoi
        with:
          bin-dir: /usr/local/bin

      - name: Create test config from fixtures (multi account, packages off)
        run: bash tests/fixtures/chezmoi/compose.sh multi off --out ~/.config/chezmoi/chezmoi.toml

      - name: Run chezmoi apply
        run: |
          chezmoi apply --source="${GITHUB_WORKSPACE}" --verbose

      - name: Verify core files exist
        run: |
          echo "=== Checking generated files ==="
          test -f ~/.gitconfig && echo "✓ ~/.gitconfig exists" || exit 1
          test -f ~/.zshrc && echo "✓ ~/.zshrc exists" || exit 1
          test -f ~/.tmux.conf && echo "✓ ~/.tmux.conf exists" || exit 1
          test -f ~/.aliases.zsh && echo "✓ ~/.aliases.zsh exists" || exit 1
          test -d ~/.config/nvim && echo "✓ ~/.config/nvim exists" || exit 1
          echo "=== All core files present ==="

  #=============================================================================
  # TIER 2: Integration Tests - Validate account system generates correct output
  #=============================================================================
  integration-test:
    name: Integration Test (Account System)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.heavy == 'true'
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: ./.github/actions/install-chezmoi

      - name: Create test config from fixtures (multi account, packages off)
        run: bash tests/fixtures/chezmoi/compose.sh multi off --out ~/.config/chezmoi/chezmoi.toml

      - name: Run chezmoi apply
        run: ~/.local/bin/chezmoi apply --source="${GITHUB_WORKSPACE}"

      - name: Test .gitconfig has correct includeIf rules
        run: |
          echo "=== Testing .gitconfig includeIf rules ==="

          # Check includeIf for alice's directories
          grep -F "includeIf \"gitdir/i:$HOME/projects/alice/\"" ~/.gitconfig && echo "✓ includeIf for alice/projects" || exit 1
          grep -F "includeIf \"gitdir/i:$HOME/.dotfiles/\"" ~/.gitconfig && echo "✓ includeIf for .dotfiles" || exit 1

          # Check includeIf for bob's directories
          grep -F "includeIf \"gitdir/i:$HOME/projects/bobcorp/\"" ~/.gitconfig && echo "✓ includeIf for bobcorp" || exit 1

          echo "=== includeIf rules correct ==="

      - name: Test .gitconfig has correct URL shortcuts
        run: |
          echo "=== Testing .gitconfig URL shortcuts ==="

          # Check URL rewriting for alice
          grep -q 'url "git@github-alice:alice/"' ~/.gitconfig && echo "✓ URL shortcut for alice" || exit 1
          grep -q 'insteadOf = https://github.com/alice/' ~/.gitconfig && echo "✓ insteadOf for alice" || exit 1

          # Check URL rewriting for bob
          grep -q 'url "git@github-bob:bobcorp/"' ~/.gitconfig && echo "✓ URL shortcut for bob" || exit 1
          grep -q 'insteadOf = https://github.com/bobcorp/' ~/.gitconfig && echo "✓ insteadOf for bobcorp" || exit 1

          echo "=== URL shortcuts correct ==="

      - name: Test SSH config has correct host entries
        run: |
          echo "=== Testing SSH config ==="

          grep -q 'Host github-alice' ~/.ssh/config && echo "✓ Host github-alice exists" || exit 1
          grep -q 'IdentityFile ~/.ssh/id_alice' ~/.ssh/config && echo "✓ IdentityFile for alice" || exit 1

          grep -q 'Host github-bob' ~/.ssh/config && echo "✓ Host github-bob exists" || exit 1
          grep -q 'IdentityFile ~/.ssh/id_bob' ~/.ssh/config && echo "✓ IdentityFile for bob" || exit 1

          echo "=== SSH config correct ==="

      - name: Test identity generation script exists
        run: |
          echo "=== Checking identity generation ==="
          # The run_onchange script should have created identity files
          # Note: In CI the script may not run, but we can at least check the template output
          ~/.local/bin/chezmoi execute-template --source="${GITHUB_WORKSPACE}" '{{ range .accounts }}{{ if hasKey . "username" }}{{ .provider | default "github" }}-{{ .username }},{{ end }}{{ end }}' | grep -q 'github-alice' && echo "✓ Template can iterate accounts" || exit 1
          echo "=== Identity generation verified ==="

      - name: Summary
        run: |
          echo ""
          echo "============================================"
          echo "  ✅ All integration tests passed!"
          echo "============================================"
          echo ""
          echo "Validated:"
          echo "  • Multi-account config parsing"
          echo "  • .gitconfig includeIf generation"
          echo "  • .gitconfig URL shortcuts generation"
          echo "  • SSH config host entries"
          echo "  • Template iteration over accounts array"

  #=============================================================================
  # TIER 3: Backward Compatibility - Test with minimal/legacy configs
  #=============================================================================
  minimal-config-test:
    name: Minimal Config Test (Backward Compat)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.heavy == 'true'
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install zsh
        run: sudo apt-get update && sudo apt-get install -y zsh

      - name: Create test config from fixtures (minimal account, packages off)
        run: bash tests/fixtures/chezmoi/compose.sh minimal off --out ~/.config/chezmoi/chezmoi.toml

      - name: Run install.sh (Universal Installer)
        run: |
          # Use the installer script to bootstrap everything
          sh install.sh

          # Add local bin to path for subsequent steps
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Verify gitconfig has user info
        run: |
          echo "=== Testing minimal config (simple mode) ==="
          grep -q 'name = "Minimal User"' ~/.gitconfig && echo "✓ user.name set" || exit 1
          grep -q 'email = "minimal@example.com"' ~/.gitconfig && echo "✓ user.email set" || exit 1
          echo "=== Minimal config works ==="

      - name: Summary
        run: |
          echo ""
          echo "============================================"
          echo "  ✅ Backward compatibility test passed!"
          echo "============================================"
          echo ""
          echo "Validated:"
          echo "  • Configs with missing key/organizations/dirs work"
          echo "  • Simple single-identity mode works"

  # TIER 3: Windows Installer Test
  #=============================================================================
  test-windows-install:
    name: Windows Installer Test
    runs-on: windows-latest
    needs: changes
    if: needs.changes.outputs.heavy == 'true'
    # 20 not 15 (#132): the installer runs Chocolatey and the whole apply.
    # This is also the job class that hung 40+ minutes on an unanswered
    # Read-Host before the non-interactive guard existed.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Create test config from fixtures (minimal account, packages off)
        # bash on purpose: one composer for every runner. chezmoi.exe reads the
        # config under USERPROFILE, so the path is built from that rather than
        # from bash's own HOME, which a runner may point elsewhere.
        shell: bash
        run: bash tests/fixtures/chezmoi/compose.sh minimal off --source-dir "$GITHUB_WORKSPACE" --out "$(cygpath -u "$USERPROFILE")/.config/chezmoi/chezmoi.toml"

      - name: Run install.ps1
        shell: powershell
        env:
          CHEZMOI_TEST_MINIMAL: "true"
        run: |
          # CHEZMOI_TEST_MINIMAL (checked alongside CI, which GitHub Actions already
          # sets to "true" here regardless) tells install.ps1 to skip its own
          # interactive prompts (admin-elevation question, trailing "Press Enter to
          # exit") - it has no effect on chezmoi's own template prompting, which
          # this runner's CI=true already handles on its own.
          .\install.ps1

      - name: Fix sourceDir in config
        shell: powershell
        run: |
          # install.ps1 overwrites config, so we must re-add sourceDir for verify to work
          $conf = "$env:USERPROFILE\.config\chezmoi\chezmoi.toml"
          $src = "sourceDir = '$env:GITHUB_WORKSPACE'"
          if (Test-Path $conf) {
            $content = Get-Content $conf -Raw
            Set-Content $conf "$src`n$content" -Encoding utf8
          } else {
            Write-Error "Config file not found at $conf"
            exit 1
          }
          exit 0

      - name: Verify installation
        shell: powershell
        run: |
          $env:Path = [System.Environment]::GetEnvironmentVariable("Path", "Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path", "User")
          # --exclude scripts: run_after_* scripts (dotfiles-doctor health
          # check) are perpetually "due" by design - chezmoi lists always-run
          # scripts as pending in diff/verify, so a repo carrying one can
          # never pass a bare `chezmoi verify` (reproduced with a minimal
          # fixture). File-state verification is what this step needs.
          chezmoi verify --exclude scripts
          if ($LASTEXITCODE -ne 0) { exit 1 }
          Write-Host "Windows installation verified!"
